name: Apply Beginner Auto Detect

on:
  push:
    paths:
      - 'scripts/apply-beginner-autodetect.js'
      - '.github/workflows/apply-beginner-autodetect.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply patch
        run: node scripts/apply-beginner-autodetect.js
      - name: Commit implementation
        run: |
          if git diff --quiet -- index.html; then
            echo "No implementation changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "feat: auto-detect API base URL setup"
          git push
