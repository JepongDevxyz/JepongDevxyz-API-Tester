{
  "name": "jepongdevxyz-api-tester",
  "version": "1.0.0",
  "private": true,
  "description": "AgentRouter.org API tester with direct browser and Vercel proxy modes.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "node --check api/agentrouter.js && node --check lib/agentrouter-core.js && node --check lib/proxy-core.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
